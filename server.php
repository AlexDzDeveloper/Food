<?php
$_POST = json_decode(file_get_contents("php://input"), true);//так як php не вміє напряму спілкуватися з форматом JSON, то ми його декодуємо. Якщо використовувати {XMLHttpRequest}, то дана команда НЕ ПОТРІБНА
echo var_dump($_POST);//дана команда бере дані, які нам прийшли від клієнта ($_POST), перетворює їх на рядок, і показує їх знову на клієнті. Тобто це response, який нам буде приходити від серверу